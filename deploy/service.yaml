apiVersion: v1
kind: Service
metadata:
  name: talos-state-metrics
  namespace: monitoring
  labels:
    app.kubernetes.io/name: talos-state-metrics
spec:
  selector:
    app.kubernetes.io/name: talos-state-metrics
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
