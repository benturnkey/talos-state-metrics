apiVersion: v1
kind: Secret
metadata:
  name: talos-state-metrics-reader
  namespace: monitoring
  labels:
    app.kubernetes.io/name: talos-state-metrics
type: Opaque
stringData:
  config: |
    # Replace with a Talos client config generated with RBAC role os:reader.
    # Do not commit real Talos client configs or private keys.
    context: talos-state-metrics-reader
    contexts: {}
